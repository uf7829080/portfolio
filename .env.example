# Copy to .env.local and set to switch the contact form from demo mode to live.
# Works with any endpoint that accepts a JSON POST (Formspree, a serverless
# function, your own API).
VITE_CONTACT_ENDPOINT=
